/** Horizontal margin ratio used to visually separate adjacent hex columns. */
export const BOARD_HORIZONTAL_MARGIN_RATIO = 0.1;

/** Vertical multiplier used to offset side columns in the pointy-top layout. */
export const BOARD_COLUMN_VERTICAL_OFFSET_RATIO = 1.1;

/** Pixel alignment offset shared by board coordinates and panel placement. */
export const BOARD_PANEL_VERTICAL_PIXEL_OFFSET = 5;

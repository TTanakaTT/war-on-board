/** Horizontal distance between token centers in a panel row. */
export const PIECE_TOKEN_CENTER_SPACING = 38;

/** Duration of the moving token preview animation in milliseconds. */
export const PIECE_PREVIEW_ANIMATION_MS = 1600;

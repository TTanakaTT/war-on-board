---
description: "Run pre-commit checks: format, lint, type-check, and unit tests"
---

Run the following commands sequentially. Stop immediately if any command fails and report the error.

1. `pnpm format`
2. `pnpm lint`
3. `pnpm check`
4. `pnpm test`

Report a summary of each step (pass/fail) when complete.
